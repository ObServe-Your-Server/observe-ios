name: iOS starter workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Test default scheme using custom iPhone simulator
    runs-on: macos-14

    steps:
      - name: Set up Xcode 16
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16'

      - name: Checkout
        uses: actions/checkout@v4

      - name: Boot iOS Simulator
        id: boot-sim
        run: |
          echo "🔍 Searching runtime..."
          RUNTIME=$(xcrun simctl list runtimes | grep -o "com.apple.CoreSimulator.SimRuntime.iOS-17-4" | head -1)
          if [ -z "$RUNTIME" ]; then
            echo "❌ Could not find iOS 17.4 runtime."
            exit 1
          fi

          DEVICE_TYPE="com.apple.CoreSimulator.SimDeviceType.iPhone-14"
          echo "📱 Creating iPhone 14 simulator with runtime: $RUNTIME"
          UDID=$(xcrun simctl create "CI-iPhone14" "$DEVICE_TYPE" "$RUNTIME")

          echo "🚀 Booting simulator $UDID"
          xcrun simctl boot "$UDID"

          echo "⏳ Waiting for simulator to become available..."
          timeout=30
          while ! xcrun simctl list devices | grep "$UDID" | grep -q "Booted"; do
            sleep 1
            timeout=$((timeout - 1))
            if [ $timeout -le 0 ]; then
              echo "❌ Simulator did not boot in time"
              exit 1
            fi
          done

          echo "✅ Simulator is ready: $UDID"
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV
          echo "$UDID" > simulator_udid.txt

      - name: Set Default Scheme
        run: |
          scheme_list=$(xcodebuild -list -json | tr -d "\n")
          if [[ -z "$scheme_list" ]]; then
            echo "❌ Error: Could not retrieve scheme list. Possibly invalid project or Xcode mismatch."
            exit 1
          fi
          default=$(echo "$scheme_list" | ruby -e "require 'json'; puts JSON.parse(STDIN.gets)['project']['targets'][0]")
          echo "$default" > default
          echo "✅ Using default scheme: $default"

      - name: Build
        env:
          scheme: ${{ 'default' }}
        run: |
          if [ "$scheme" = "default" ]; then scheme=$(cat default); fi
          if ls -A | grep -iE '\.xcworkspace$' > /dev/null; then
            filetype_parameter="workspace"
            file_to_build=$(ls -A | grep -iE '\.xcworkspace$' | head -1)
          else
            filetype_parameter="project"
            file_to_build=$(ls -A | grep -iE '\.xcodeproj$' | head -1)
          fi
          file_to_build=$(echo "$file_to_build" | awk '{$1=$1;print}')
          echo "📦 Building $filetype_parameter: $file_to_build with scheme: $scheme"
          xcodebuild build-for-testing -scheme "$scheme" -"$filetype_parameter" "$file_to_build" -destination "id=$SIMULATOR_UDID"

      - name: Test
        env:
          scheme: ${{ 'default' }}
        run: |
          if [ "$scheme" = "default" ]; then scheme=$(cat default); fi
          if ls -A | grep -iE '\.xcworkspace$' > /dev/null; then
            filetype_parameter="workspace"
            file_to_build=$(ls -A | grep -iE '\.xcworkspace$' | head -1)
          else
            filetype_parameter="project"
            file_to_build=$(ls -A | grep -iE '\.xcodeproj$' | head -1)
          fi
          file_to_build=$(echo "$file_to_build" | awk '{$1=$1;print}')
          echo "🧪 Testing $filetype_parameter: $file_to_build with scheme: $scheme"
          xcodebuild test-without-building -scheme "$scheme" -"$filetype_parameter" "$file_to_build" -destination "id=$SIMULATOR_UDID"

      - name: Shutdown & delete simulator
        if: always()
        run: |
          if [ -f simulator_udid.txt ]; then
            UDID=$(cat simulator_udid.txt)
            echo "🧹 Shutting down and deleting simulator $UDID"
            xcrun simctl shutdown "$UDID" || true
            xcrun simctl delete "$UDID" || true
          fi
